{
  age = {
    secrets = {
      secret1 = {
        file = ../../secrets/secret1.age;
      };
      n00r-secrets = {
        file = ../../secrets/n00r-secrets.age;
        owner = "n00r";
      };

    };
  };
}
